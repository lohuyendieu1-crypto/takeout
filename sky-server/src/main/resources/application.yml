server:
  port: 8080

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity
  configuration:
    #開啟駝峰命名
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

sky:
  jwt:
    # 設置 jwt 簽名加密時使用的密鑰
    admin-secret-key: itcast
    # 設置 jwt 過期時間
    admin-ttl: 7200000
    # 設置前端傳遞過來的令牌名稱
    admin-token-name: token

cloudflare:
  r2:
    access-key: ${sky.r2.access-key}
    secret-key: ${sky.r2.secret-key}
    # 注意：Endpoint 只要填到 .com 結尾，不要在後面加 bucket 名字
    endpoint: ${sky.r2.endpoint}
    bucket-name: ${sky.r2.bucket-name}
    # 這是你在 R2 後台開通 Public Access 後拿到的那個網址
    domain: ${sky.r2.domain}